const User = require('../models/User');
const Task = require("../models/Task");
const TaskDetail = require("../models/TaskDetail");
const Schedule = require("../models/Schedule");

module.exports = { User, Task, TaskDetail, Schedule };